      <details className="widget-panel widget-panel--media">
        <summary className="widget-panel-summary">
          <span>Media Ingestion</span>
          <span className="widget-panel-aside">
            {mediaTotal > 0 ? `${fmt(mediaProcessed)} / ${fmt(mediaTotal)}` : '—'}
          </span>
        </summary>

        <div className="progress-wrap">
          <div className="track">
            <div
              className="fill fill--media"
              style={{
                width: `${Math.max(mediaPctSafe, mediaPctSafe > 0 ? 0.4 : 0)}%`,
              }}
            >
              <div className="fill-shimmer" />
            </div>
          </div>
          <span className="pct">{mediaTotal > 0 ? mediaPctSafe.toFixed(1) : '—'}%</span>
        </div>
      </details>
