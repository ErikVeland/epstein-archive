
  .ingest-pill {
    font-size: 10px;
    font-weight: 600;
    padding: 2px 8px;
    border-radius: 999px;
    border: 1px solid rgba(255,255,255,0.10);
    color: rgba(255,255,255,0.60);
    background: rgba(255,255,255,0.05);
    letter-spacing: 0.02em;
    text-transform: lowercase;
  }

  .ingest-pill--on {
    color: rgba(48, 209, 88, 0.90);
    border-color: rgba(48, 209, 88, 0.22);
    background: rgba(48, 209, 88, 0.08);
  }

  .ingest-pill--off {
    color: rgba(255,255,255,0.32);
  }

  .controls {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 6px;
    margin: 8px 0 2px;
  }

  .ctl {
    appearance: none;
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(255,255,255,0.06);
    color: rgba(255,255,255,0.82);
    border-radius: 12px;
    padding: 7px 10px;
    font-size: 12px;
    font-weight: 600;
    letter-spacing: -0.01em;
    cursor: pointer;
    box-shadow: inset 0 1px 0 rgba(255,255,255,0.08), 0 2px 8px rgba(0,0,0,0.18);
  }

  .ctl:disabled {
    opacity: 0.45;
    cursor: not-allowed;
  }

  .ctl--green {
    border-color: rgba(48, 209, 88, 0.22);
    background: rgba(48, 209, 88, 0.10);
    color: rgba(48, 209, 88, 0.90);
  }

  .ctl--red {
    border-color: rgba(255, 69, 58, 0.24);
    background: rgba(255, 69, 58, 0.10);
    color: rgba(255, 69, 58, 0.90);
  }

  .fill--media {
    background: linear-gradient(90deg, #ff9f0a 0%, #ffd60a 100%);
    box-shadow: 0 0 10px rgba(255, 159, 10, 0.45), 0 0 3px rgba(255, 214, 10, 0.40);
  }

  .exo-row {
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 8px;
    margin: 8px 0 2px;
  }

  .exo-pill {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.06em;
    padding: 3px 8px;
    border-radius: 999px;
    border: 1px solid rgba(10,132,255,0.22);
    background: rgba(10,132,255,0.10);
    color: rgba(10,132,255,0.88);
  }

  .exo-model {
    flex: 1;
    font-size: 11px;
    color: rgba(255,255,255,0.45);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .exo-link {
    font-size: 11px;
    font-weight: 600;
    color: rgba(255,255,255,0.70);
    text-decoration: none;
    border: 1px solid rgba(255,255,255,0.10);
    background: rgba(255,255,255,0.06);
    padding: 5px 8px;
    border-radius: 10px;
  }

  .stage-grid {
    position: relative;
    z-index: 2;
    display: grid;
    gap: 5px;
    margin: 8px 0 8px;
  }

  .stage-row {
    display: grid;
    grid-template-columns: 8px minmax(0, 1fr) auto;
    align-items: center;
    gap: 7px;
    min-height: 24px;
    padding: 4px 7px;
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 10px;
    background: rgba(255,255,255,0.045);
  }

  .stage-dot {
    width: 7px;
    height: 7px;
    border-radius: 999px;
    background: rgba(255,255,255,0.22);
    box-shadow: 0 0 0 3px rgba(255,255,255,0.03);
  }

  .stage-row--running .stage-dot {
    background: #0a84ff;
    box-shadow: 0 0 10px rgba(10,132,255,0.55);
  }

  .stage-row--done .stage-dot {
    background: #30d158;
    box-shadow: 0 0 10px rgba(48,209,88,0.45);
  }

  .stage-row--failed .stage-dot {
    background: #ff453a;
    box-shadow: 0 0 10px rgba(255,69,58,0.45);
  }

  .stage-name {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    font-size: 11px;
    color: rgba(255,255,255,0.72);
  }

  .stage-count {
    font-size: 10px;
    font-variant-numeric: tabular-nums;
    color: rgba(255,255,255,0.42);
  }

  .artifact-strip {
    position: relative;
    z-index: 2;
    display: flex;
    justify-content: space-between;
    gap: 8px;
    margin: 0 0 2px;
    font-size: 10px;
    color: rgba(255,255,255,0.44);
  }

  .widget-panel {
    position: relative;
    z-index: 2;
    margin: 5px 0;
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 10px;
    background: rgba(2, 22, 43, 0.38);
    overflow: hidden;
  }

  .widget-panel[open] {
    background: rgba(2, 22, 43, 0.52);
  }

  .widget-panel-summary {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, auto) 10px;
    align-items: center;
    gap: 8px;
    min-height: 24px;
    padding: 4px 8px;
    color: rgba(255,255,255,0.72);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    cursor: pointer;
    list-style: none;
    user-select: none;
  }

  .widget-panel-summary::-webkit-details-marker {
    display: none;
  }

  .widget-panel-summary::after {
    content: '▾';
    color: rgba(255,255,255,0.35);
    font-size: 10px;
    transform: rotate(-90deg);
    transition: transform 140ms ease;
  }

  .widget-panel[open] > .widget-panel-summary::after {
    transform: rotate(0deg);
  }

  .widget-panel-summary > span:first-child {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .widget-panel-aside {
    min-width: 0;
    max-width: 145px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
    justify-self: end;
    color: rgba(255,255,255,0.42);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0;
    text-transform: none;
  }

  .widget-panel[open] > .widget-panel-summary {
    border-bottom: 1px solid rgba(255,255,255,0.06);
  }
