<details className="widget-panel widget-panel--stages">
  <summary className="widget-panel-summary">
    <span>Unified Pipeline</span>
    <span className="widget-panel-aside">
      {activeStage ? `${activeStage} · ${runtimeStatus}` : `${fmt(aiArtifacts)} AI artifacts`}
    </span>
  </summary>

  <div className="stage-grid">
    {stageRows.map((stage) => (
      <div key={stage.name} className={`stage-row stage-row--${stage.state}`}>
        <span className="stage-dot" />
        <span className="stage-name">{stage.label}</span>
        <span className="stage-count">
          {fmt(stage.done)} / {fmt(stage.total)}
        </span>
      </div>
    ))}
  </div>

  {['blocked', 'stalled', 'stale'].includes(runtimeStatus) && (
    <div className="pipeline-alert">
      {data.runtime?.blockedReason ||
        data.pipeline_status?.runtime?.blockedReason ||
        data.blockedReason ||
        `Pipeline ${runtimeStatus}; no recent successful enrichment output.`}
    </div>
  )}

  <div className="artifact-strip">
    <span>AI artifacts {fmt(aiArtifacts)}</span>
    <span>semantic docs {fmt(semanticDocumentEmbeddings)}</span>
  </div>
</details>;
