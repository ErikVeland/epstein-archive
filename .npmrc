; Network hardening for CI/agents (prevents flaky socket timeouts during large installs)
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=600000

; Ensure a stable store location inside the repo (avoids mismatched global store paths)
store-dir=.pnpm-store
